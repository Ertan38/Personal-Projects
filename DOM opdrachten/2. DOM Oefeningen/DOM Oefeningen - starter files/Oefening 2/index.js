const imageSrc = 'logoipsum-277.svg';

const image = document.createElement('img');

image.src = imageSrc;

const imageContainer = document.getElementById('main');

imageContainer.appendChild(image);
